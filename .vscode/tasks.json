{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Current File",
            "type": "shell",
            "command": "java -jar tools/KickAss.jar -o build/${fileBasenameNoExtension}.prg ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
